<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="com.example.android.phonebook.EditActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="128dp">
        <com.example.android.phonebook.RoundedImageView
            android:scaleType="centerCrop"
            android:onClick="captureImageInitialization"
            android:id="@+id/edit_image_view"
            android:src="@drawable/dummy_user"
            android:layout_width="128dp"
            android:layout_height="128dp" />
        <EditText
            android:id="@+id/name_edit_view"
            android:padding="8dp"
            android:layout_toRightOf="@id/edit_image_view"
            android:textSize="36sp"
            android:text="Nama"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />
    </RelativeLayout>

    <EditText
        android:id="@+id/number_edit_view"
        android:layout_margin="24sp"
        android:gravity="center"
        android:text="08XXXXXXXX"
        android:textSize="36sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <Button
            android:onClick="submitEdit"
            android:id="@+id/edit_button_submit"
            android:layout_weight="1"
            android:text="Submit"
            android:gravity="center"
            android:layout_width="0dp"
            android:layout_height="wrap_content" />

        <Button
            android:onClick="cancelButton"
            android:id="@+id/edit_button_cancel"
            android:layout_weight="1"
            android:text="Cancel"
            android:gravity="center"
            android:layout_width="0dp"
            android:layout_height="wrap_content" />


    </LinearLayout>

    <TextView
        android:id="@+id/posisi"
        android:text="posisi"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</LinearLayout>
